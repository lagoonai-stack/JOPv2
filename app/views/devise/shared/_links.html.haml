- if controller_name != "sessions"
  = link_to t("auth.log_in_link"), new_session_path(resource_name)
- if devise_mapping.registerable? && controller_name != "registrations"
  = link_to t("auth.sign_up_link"), new_registration_path(resource_name)
